version: '3'

services:
  hns-backend:
    build: ./backend
    command: flask run --host=0.0.0.0
    restart: always
    ports:
      - "5000:5000"
    

  hns-frontend:
    build: ./frontend
    command: npm start
    restart: always
    ports:
      - "3000:3000"
